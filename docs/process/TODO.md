# Task Board

> Persistent task tracking that survives across sessions. Agents should check this before starting work and update it when finishing.

## Priorities

- **P0 - Critical:** Blocking progress, fix immediately
- **P1 - High:** Important for current milestone
- **P2 - Medium:** Should be done soon
- **P3 - Low:** Nice to have, do when convenient

## Current Milestone

**Milestone:** Milestone 3 - Church Claim & Admin Ownership  
**Target Date:** TBD  
**Goal:** Let church representatives claim listings and unlock the church-admin workflow that will power event creation/editing and future listing-management tools.
**Roadmap Note:** The temporary MVP-demo detour is complete. Keep Milestone 3 moving forward from the shipped public events foundation, while still tracking the live auth deployment regression as a separate production blocker.

---

## Tasks

### P0 - Critical

- [ ] **Operational follow-ups for the 2026-07-13 fix pass** (in order, after merging `claude/church-finder-improvements-9budfk`):
  1. Redeploy `sa-church-finder-api` on Render (also resolves the long-standing auth-500 item below). `migrate deploy` in the build will add the new forum indexes automatically.
  2. Redeploy the static site so the perf/ribbon/passport/forum client fixes go live.
  3. `DATABASE_URL=<prod> npm run db:seed:ribbon` — restores the category ribbon icons (idempotent, upsert-only).
  4. `DATABASE_URL=<prod> npm run enrich:church-details -- --reclassify-denominations --dry-run` (review, then run without `--dry-run`) — corrects the LDS-temple-as-Catholic misclassification so the new ranking demotion takes effect.
  5. Consider a paid Render instance for the API; the new keepalive workflow only mitigates free-tier cold starts.
- [ ] Restore live auth/session flows on the deployed app by redeploying the backend with the Prisma-backed session-store fix - the rerun from `https://sachurchfinder.com` now shows CORS is healthy, but `POST /api/v1/auth/register` and `POST /api/v1/auth/login` still return `500`, which keeps saves and reviews blocked. _Status 2026-06-09: the session-store fix (`server/src/lib/prisma-session-store.ts`) has long been merged to `main` and all repo gates are green, so the only remaining action is operational — trigger a manual deploy of `sa-church-finder-api` in the Render dashboard (or confirm auto-deploy ran) and retest register/login on the live site. This cannot be performed from the cloud agent sandbox (no Render access; outbound requests to `sachurchfinder.com` are blocked by the egress policy)._
- [x] Set up PostgreSQL + PostGIS database, run Prisma migrations, seed database - Supabase cloud (us-west-2), completed 2026-03-28
- [x] **Deploy to Render** - backend + frontend live on Render, env wiring verified, and live site smoke-tested end-to-end (2026-03-28)
- [x] Run MVP baseline audit (`lint`, `typecheck`, `test`, `build`) - completed 2026-03-30

### P1 - High

- [x] Fix the flaky `html-cache` Jest test (audit 2026-06-09): `readHtmlCache` computes `Date.now() - stat().mtimeMs`, which can come out slightly negative (integer now vs fractional mtime), so the `maxAgeMs: 0` case intermittently returns cached HTML instead of `null` — it failed in the full-suite run (622/623) but passes 8/8 in isolation; guard `maxAgeMs <= 0` (or clamp the age) at `server/src/scripts/enrich-website-v2/html-cache.ts:36` — **Done 2026-06-09:** `readHtmlCache` now short-circuits `maxAgeMs <= 0` before the mtime comparison, so the 0-ms case is deterministic.
- [x] Verify and fix the production CSP image allowlist: both `render.yaml` and the stale `client/vercel.json` set `img-src 'self' data: blob:` + Mapbox only, but the homepage hardcodes `images.unsplash.com` photos (`client/src/pages/HomePage.tsx`, `client/src/index.css`) and church `coverImageUrl`/gallery photos are Supabase storage URLs (`photo-pipeline.ts`) — those hosts are missing from `img-src`, so images are very likely blocked on the live site; add `https://images.unsplash.com` and the Supabase project host, then verify live (sandbox egress blocks `sachurchfinder.com`) — **Config fixed 2026-06-09:** `render.yaml` `img-src` now allows `https://images.unsplash.com` and `https://*.supabase.co` (the stale `client/vercel.json` was deleted). Still needs a live-site spot check after the next Render deploy.
- [x] Fix sitemap wiring for SEO: `client/public/robots.txt` points to `https://sachurchfinder.com/sitemap.xml`, which serves the hardcoded static `client/public/sitemap.xml` (no church profile URLs); the dynamic per-church sitemap (`server/src/routes/sitemap.routes.ts`) lives on the API domain and is unreachable at that URL — pick one source (rewrite/proxy to the API, or generate at build time) and delete the loser — **Done 2026-06-09:** deleted the static `client/public/sitemap.xml`; `robots.txt` now declares the API-generated sitemap (`https://sa-church-finder-api.onrender.com/sitemap.xml`, valid cross-host submission), which includes every church profile.
- [x] Remove or replace `.github/workflows/preview.yml`: it deploys PR previews to Vercel, but hosting is Render and the workflow has failed on all 9 PR runs to date — every PR shows a red ❌ — **Done 2026-06-09:** deleted the workflow; `ci.yml` no longer references the nonexistent `develop` branch and `deploy.yml` placeholders now point at Render instead of Vercel.
- [x] Add real rate limiting to the API (at minimum `/auth/login`, `/auth/register`, and the password-reset/verification endpoints): no rate limiting exists anywhere in `server/src` despite AGENTS.md and API_SPEC.md claiming it does — **Done 2026-06-09:** new in-house fixed-window limiter (`server/src/middleware/rate-limit.ts`, no new dependency) applies a shared 5-req/min/IP budget across register, login, forgot/reset-password, verify-email(+resend), and change-password; 429 + RateLimit/Retry-After headers; disabled under Jest; covered by unit tests; API_SPEC rate-limit section rewritten to match.
- [x] Build church-admin event creation/edit tools now that approved claims can establish church-admin ownership - church admins and site admins can now create, update, and delete events for churches they manage via the Leaders Portal, backed by new `/api/v1/churches/:churchId/events` POST and `/api/v1/events/:id` PATCH/DELETE endpoints (2026-04-11)
- [x] Add aggregated events discovery/feed page after church-admin event tools are live - added `GET /api/v1/events` aggregated feed with type/date/keyword filters, pagination, and church info joins, plus a public `/events` discovery page with URL-synced filters, empty/loading/error states, pagination, and new Header + Footer + Sitemap nav entries (2026-04-11)
- [x] Build Milestone 3 church claim request flow - added member-facing claim submission on unclaimed church profiles, account-page claim history, a site-admin approval queue, and server/client coverage (2026-03-31)
- [ ] Smoke test the current live flows end to end on the deployed app (search, profile, auth, saves, reviews, events) - _blocked from the cloud agent sandbox (egress policy denies `sachurchfinder.com`); needs a human or an environment with open network access, ideally right after the Render backend redeploy above_
- [x] Curate a gold set of 12 real San Antonio churches for the polished MVP dataset - the seed now uses a curated 12-church hero set with real cover images and only high-confidence profiles (2026-03-30)
- [x] Refine church profiles so the shortlisted records have trustworthy descriptions, contact info, website links, service details, and clean metadata - completed for the current 12-church gold set with real cover images and honest zero-review baselines (2026-03-30)
- [x] Review the current seeded/live dataset for rough copy, inconsistent naming, weak images, and missing fields that hurt the demo - seed audit documented in `docs/product/data/MVP_DATASET_AUDIT.md` (2026-03-30)
- [x] Prepare the launch-ready deployment checklist for a shareable domain (Render settings, env vars, callback URLs, and DNS/custom-domain wiring) - user confirmed `sachurchfinder.com` is live; repo defaults updated for the custom frontend origin (2026-03-30)
- [x] Replace placeholder auth API with real local session-based auth (`register`, `login`, `logout`, `me`) - completed 2026-03-28
- [x] Add auth UI and client-side current-session integration - completed 2026-03-28
- [x] Build saved churches MVP (save/unsave on cards + profile, account list) - completed 2026-03-28
- [x] Build reviews MVP (create/edit review flow, church profile list, account history) - completed 2026-03-28
- [x] Build forgot/reset password flow (reset-token APIs, frontend pages, and opt-in local preview link) - completed 2026-03-28
- [x] Build email verification flow (token APIs, verification page, account resend action, and opt-in local preview link) - completed 2026-03-28
- [x] Finish auth follow-up: Google OAuth - completed 2026-03-29
- [x] Build Church Profile Page (F1.3 - hero, about, services, location, contact) - completed 2026-03-27
- [x] Integrate Mapbox GL JS when token is available (replace MapPlaceholder) - completed 2026-03-27
- [x] Implement map pin clustering for zoomed-out views - completed 2026-03-27
- [x] Add viewport-based querying (update results as map pans) - completed 2026-03-27
- [x] Make URL reflect search state (shareable/bookmarkable) - completed 2026-03-27

### P2 - Medium

- [x] Fix the PWA icon set (audit 2026-06-09): `client/public/manifest.json` references `/icons/icon-192.png`, `/icons/icon-512.png`, and two maskable PNGs, and `index.html`'s apple-touch-icon references `/icons/icon-192.png`, but only `icons/icon.svg` exists — generate the PNGs (or point the manifest at the SVG), and delete the orphaned `client/public/site.webmanifest` (never linked from `index.html`, references root-level icons that also don't exist) — **Done 2026-06-09:** generated real `icon-192/512.png` and full-bleed maskable variants from `icon.svg` (sharp), so `manifest.json` and the apple-touch-icon now resolve; deleted the orphaned `site.webmanifest`.
- [x] Add a production guard for `SESSION_SECRET`: `server/src/lib/session.ts` silently falls back to `'dev-session-secret'` when the env var is unset — fail fast at startup in production (Render generates one today, so this is defense in depth) — **Done 2026-06-09:** `createSessionMiddleware` now throws at startup when `NODE_ENV=production` and `SESSION_SECRET` is unset; covered by a session test.
- [x] Sync API_SPEC.md with the implemented API: document `GET /analytics/churches/:churchId`, `GET /analytics/my-churches`, `GET /churches/:id/admins`, `DELETE /churches/:id/admins/:adminUserId`, and `PATCH /admin/flagged-reviews/:id`; and either implement or drop the documented-but-missing `POST /churches` — **Done 2026-06-09:** documented the analytics, church-admins, and flagged-review-resolution endpoints; dropped the never-implemented `POST /churches`.
- [x] Sync DATA_MODELS.md with `schema.prisma`: the `enrichment_states` table is undocumented, and `Church.languages` default differs (docs say `['English']`, schema says `[]`) — **Done 2026-06-09:** fixed the `Church.languages` default (`[]`); note the earlier audit was wrong about `enrichment_states` — it was already documented.
- [x] Fix stale infrastructure/docs claims: AGENTS.md repo map advertises rate-limiting middleware that doesn't exist; BRANCHING_STRATEGY.md, `ci.yml` triggers, and the `render.yaml` staging service + staging database all reference a `develop` branch that doesn't exist (the staging blueprint entries are dead config) — **Done 2026-06-09:** removed the dead staging service + database from `render.yaml`, removed `develop` from `ci.yml`, rewrote BRANCHING_STRATEGY.md for the real feature→main flow, updated ARCHITECTURE.md, and dropped the now-vestigial `*.vercel.app` CORS allowance from `app.ts` (AGENTS.md’s rate-limiting claim is now true).
- [x] Delete dead code (audit 2026-06-09): `server/src/data/churches.ts` (1,198-line pre-database artifact, zero imports), `client/vercel.json` (stale Vercel-era config superseded by `render.yaml`), the broken root `db:refresh-curated-data` script (its target `server/prisma/refresh-curated-church-data.ts` doesn't exist), and rename `server/src/routes/saved-church.routes.test.ts` to match where the routes actually live (`church.routes.ts` / `users.routes.ts`) — **Done 2026-06-09:** deleted `server/src/data/churches.ts`, `client/vercel.json`, and the broken `db:refresh-curated-data` script; renamed the test to `saved-church.integration.test.ts`.
- [x] Document env vars: there is no `client/.env.example` (`VITE_API_URL`, `VITE_MAPBOX_TOKEN`, `VITE_SENTRY_DSN`/`ENVIRONMENT`/`RELEASE`, `VITE_STRIPE_DONATION_URL`, `VITE_SUPPORT_EMAIL` are undocumented); server `.env.example` is missing `SESSION_COOKIE_SAME_SITE`, `APP_VERSION`, and `GIT_COMMIT`, and its `MAX_FILE_SIZE=5242880` (5 MB) doesn't match the 10 MB limits actually enforced in code — **Done 2026-06-09:** added `client/.env.example` covering all `VITE_*` vars; server `.env.example` gained `SESSION_COOKIE_SAME_SITE`, `APP_VERSION`, `GIT_COMMIT` and dropped the unused AWS S3 / `UPLOAD_DIR` / `MAX_FILE_SIZE` entries (upload path + 10 MB limit are hardcoded and now noted as such).
- [x] Footer UX dead-ends: eight footer links ("Volunteer opportunities", "Historic missions", "Megachurches", "Community churches", "About us", "Careers", "Blog", "San Antonio churches") render as buttons that navigate nowhere, and the globe/language-selector buttons do nothing — route them, stub pages, or remove them (`client/src/components/layout/Footer.tsx`) — **Done 2026-06-09:** every footer entry is now a real route (discovery links go to `/search` queries; product links to `/leaders`, `/compare`, `/passport`, `/account`); removed About/Careers/Blog/Volunteer dead ends and the fake language selector; Footer tests updated.
- [x] Configure real SMTP provider credentials in each live environment so auth emails send outside local development - user confirmed live setup completed 2026-03-30
- [x] Expand recurring-event handling beyond stored RRULE metadata - added an in-house RRULE parser/expander (`server/src/lib/recurrence.ts`) that supports `FREQ=DAILY|WEEKLY|MONTHLY`, `INTERVAL`, `BYDAY`, `COUNT`, and `UNTIL`; events are validated and canonicalized on write and expanded into per-occurrence payloads (with new `occurrenceId` / `seriesStartTime` / `isOccurrence` fields) on read for both the church profile feed and the aggregated `/events` feed, plus a recurrence picker on the admin Event form and `expand=false` for the Leaders Portal series view (2026-04-11)
- [x] Launch church events foundation on church profile pages (public events API, type/date filters, upcoming-this-week summary, and seeded sample events) - completed 2026-03-30
- [x] Refresh account page UX copy and empty states so the signed-in experience feels member-facing instead of reading like an internal milestone checklist - completed 2026-03-29
- [x] Add responsive layout (mobile tabs for map/list) - completed 2026-03-27
- [x] Add loading skeleton animations to ChurchCard - shimmer effect component, completed 2026-03-27
- [x] Implement "no results" suggestions (widen radius, change filters) - completed 2026-03-27
- [x] Write database seed script with proper PostGIS location column - completed 2026-03-27
- [x] Upgrade deprecated deps (`eslint` 8 -> 9, `multer` 1 -> 2, `supertest` 6 -> 7) - completed 2026-03-28
- [x] Add review helpful voting follow-up after the MVP - completed 2026-03-28
- [x] Add review moderation follow-ups after the MVP - completed 2026-03-29
- [x] Wire real transactional email delivery for auth emails (password reset + verification) - completed 2026-03-29
- [x] Review lazy-loaded Mapbox bundle size warning and optimize the production bundle by runtime-loading Mapbox GL from CDN instead of bundling the library - completed 2026-03-30

### P3 - Low

- [ ] Clean up the 48 stale `claude/*` remote branches — **Attempted 2026-06-09, blocked by sandbox:** the cloud session's push proxy only allows pushing to the session's own branch (ref deletions return HTTP 403), so this needs a human or a fully-credentialed environment. 24 branches are provably merged (their commits are ancestors of `main`) and safe to delete with:

  ```bash
  git push origin --delete \
    claude/church-data-retrieval-GfB4b claude/confident-ritchie-CvdQn \
    claude/continue-work-fStdb claude/enrichment-pipeline-v2-38wnd \
    claude/focused-mccarthy-ybb8b2 claude/grant-admin-privileges-2JiFD \
    claude/improve-seo-moFkI claude/improve-website-performance-flZm6 \
    claude/relaxed-meitner-0mGA1 claude/relaxed-meitner-3iTU4 \
    claude/relaxed-meitner-408tA claude/relaxed-meitner-9C9K1 \
    claude/relaxed-meitner-S4MpC claude/relaxed-meitner-SEZTn \
    claude/relaxed-meitner-Xf60G claude/relaxed-meitner-YpIZt \
    claude/relaxed-meitner-ZZfVj claude/relaxed-meitner-gTPD5 \
    claude/relaxed-meitner-gcgZR claude/relaxed-meitner-k4XPQ \
    claude/relaxed-meitner-o4DlA claude/relaxed-meitner-pqSGg \
    claude/relaxed-meitner-q4FtX claude/relaxed-meitner-x2iPu \
    claude/user-passport-tracking-N3E2m
  ```

  The other 24 are NOT ancestors of `main` (likely squash-merged or abandoned) — review before deleting.

- [x] Add `compression` middleware to the Express API (responses currently ship uncompressed) — **Done 2026-06-09:** added `compression` (+`@types/compression`) — the one new dependency from this pass — wired before body parsing in `app.ts`.
- [x] Decide on registration enumeration: the 409 "An account with that email already exists" reveals which emails are registered — weigh enumeration resistance vs UX — **Decided 2026-06-09:** keep the clear 409 message for UX; the new 5-req/min/IP auth rate limit makes bulk enumeration impractical. Revisit only if abuse shows up.
- [ ] Plan the major-upgrade train as its own milestone: React 19, Vite 8 + Vitest 4 (clears the last four dev-only audit advisories), Prisma 7, Express 5, Tailwind 4, Zod 4, Zustand 5 are all pending majors (deferral was deliberate on 2026-06-09)
- Note: two migration pairs share identical timestamps (`20260413000000_*` ×2, `20260413100000_*` ×2); lexicographic order is currently safe and repo rules forbid renaming existing migrations — just avoid adding future migrations that assume ordering between them
- [x] Add Husky + lint-staged for pre-commit hooks - completed 2026-03-30
- [x] Set up GitHub Actions CI (lint, typecheck, test on PRs) - workflows created, CI fixes applied (2026-03-27)
- [x] Set up error monitoring (Sentry free tier) - env-gated browser + server capture landed, docs/Render wiring added (2026-03-30)
- [x] Add keyboard navigation support for search results - result cards, search submit controls, and popup CTA made keyboard-accessible (2026-03-28)

### Completed

- [x] Dependency vulnerability sweep (2026-06-09) - applied semver-compatible `npm audit fix` to both workspaces: client lockfile picked up patched `react-router`/`react-router-dom` 6.30.4 (open-redirect fix), `postcss`, `ws` 8.21.0, `brace-expansion`, and `protocol-buffers-schema`; server lockfile picked up the patched `express` 4.x line and now audits clean (0 vulnerabilities). Full gate (lint, typecheck, test, build) re-verified green afterwards
- [x] Recurring-event RRULE expansion - added `server/src/lib/recurrence.ts` with a focused iCal parser/expander, write-time validation and canonicalization inside `event.service.ts`, occurrence-aware read paths for `GET /api/v1/churches/:slug/events` and `GET /api/v1/events` (with an `expand=false` escape hatch for admins managing the series template), new `occurrenceId` / `seriesStartTime` / `isOccurrence` fields on event payloads, a recurrence picker on the Leaders Portal event form, and Jest/Vitest coverage for the library, route expansion, and UI submission (2026-04-11)
- [x] Aggregated events discovery feed - added `GET /api/v1/events` with type/date/keyword filters, pagination, and church join data; a public `/events` page with URL-synced filters, pagination, event cards linking to church profiles, empty/loading/error states, new Header + Footer + Sitemap entry points, and Jest/Vitest coverage (2026-04-11)
- [x] Milestone 3 church-admin event tools - added `POST /api/v1/churches/:churchId/events`, `PATCH /api/v1/events/:id`, `DELETE /api/v1/events/:id` with church-admin/site-admin authorization, an EventManager component on the Leaders Portal, new React Query mutations + toasts, Jest/Vitest coverage, and updated API spec (2026-04-11)
- [x] Milestone 3 church claim request flow - added `POST /api/v1/churches/:id/claim`, `GET /api/v1/users/:id/claims`, `GET/PATCH /api/v1/admin/claims`, church-profile claim UI, account-page claim history, site-admin approval actions, and focused server/client test coverage (2026-03-31)
- [x] MVP dataset narrowed to a 12-church gold set - replaced the broad shortlist with 12 high-confidence San Antonio churches, seeded real cover images for every profile, and stopped seeding synthetic reviews/events so the demo data stays trustworthy (2026-03-30)
- [x] Milestone 3 events foundation - added a public `/api/v1/churches/:slug/events` endpoint with type/date filtering, seeded upcoming events, and a church-profile events section with upcoming-this-week and next-gathering summaries (2026-03-30)
- [x] Church profile empty-media polish - replaced the fake gallery placeholders with a visit-summary hero and planning cards so church profiles still feel intentional before real cover images are curated (2026-03-30)
- [x] MVP shortlist expansion to 25 profiles - completed earlier on 2026-03-30 and later superseded by the tighter 12-church gold-set strategy for demo quality
- [x] MVP dataset seed audit - documented seed-wide data gaps, likely verification risks, and the recommended cleanup order in `docs/product/data/MVP_DATASET_AUDIT.md` (2026-03-30)
- [x] Backend integration readiness visibility - added safe `/api/v1/health` integration status for SMTP/Google/Sentry, production startup warnings for missing live credentials, and stricter SMTP partial-config validation so misconfigured auth email delivery is easier to spot before testing (2026-03-30)
- [x] Husky + lint-staged workflow polish - added root Husky hooks, staged-file ESLint/Prettier rules that reuse the existing client/server configs, a manual `npm run lint-staged` entry point, and documented the new local hook behavior in Quick Start (2026-03-30)
- [x] Error monitoring foundation - added env-gated Sentry setup for the React SPA and Express API, a browser crash fallback, server-side exception capture that skips normal 4xx app errors, Render/env example wiring, and config tests (2026-03-30)
- [x] Mapbox bundle optimization - moved Mapbox GL JS to a runtime CDN loader, aliased the bundled package to a tiny stub, removed the 1.7 MB lazy `mapbox-gl` chunk from the client build, and kept the interactive map flow unchanged (2026-03-30)
- [x] Account page UX refresh - rewrote the signed-in copy to feel member-facing, improved saved/review empty states with clearer next actions, and replaced the internal-looking sidebar checklist with user-centered guidance (2026-03-29)
- [x] Auth email delivery - added SMTP-backed password reset + email verification delivery, preserved opt-in preview links for local development, added auth email templates, Render/env wiring docs, and server auth test coverage (2026-03-29)
- [x] Search workspace UX pass - search-page toolbar with mobile search input, sort control, removable active-filter chips, slide-over advanced filters, and cleaner desktop map/list steering (2026-03-29)
- [x] Review moderation follow-up - authenticated review reporting, site-admin flagged-review queue, moderation resolution actions, and client/server coverage (2026-03-29)
- [x] Google OAuth - backend redirect/callback flow, session-safe return paths, Google account linking/creation, login/register Google CTAs, callback error messaging, and auth/client test coverage (2026-03-29)
- [x] Email verification flow - verification-token model + migration, register-time token issuance, resend + consume APIs, verification page, account resend CTA, local preview mode, and auth/client test coverage (2026-03-28)
- [x] Review helpful voting - helpful/unhelpful endpoints, viewer vote state in review payloads, church profile helpful controls, and client/server test coverage (2026-03-28)
- [x] Forgot/reset password flow - protected token issuance/consumption backend, login recovery UI, opt-in local preview link, auth route tests, and client auth API coverage (2026-03-28)
- [x] Reviews MVP - church review listing/create/edit/delete APIs, incremental aggregate rating updates, church profile review UI, account review history, and route tests (2026-03-28)
- [x] Saved churches MVP - protected save/list APIs, session-aware church payloads, save/unsave controls on cards and profile pages, account saved list, and route tests (2026-03-28)
- [x] Frontend auth UI + client session integration - login/register pages, protected account page, session-aware header, React Query auth hooks, and client auth tests (2026-03-28)
- [x] Local auth foundation - Prisma+bcrypt register/login/logout/me endpoints, session middleware, route validation, auth route tests, and testable app bootstrap (2026-03-28)
- [x] Dependency upgrade sweep - ESLint 9 flat config migration, `multer` 2.1.1, `supertest` 7.2.2, fresh client/server lockfiles, full lint/typecheck/test/build pass (2026-03-28)
- [x] Render deployment verified live - frontend and backend working on Render after env wiring + redeploy (2026-03-28)
- [x] Repository baseline re-stabilized: lint, typecheck, test, and build verified after Prisma/Render work (2026-03-28)
- [x] Supabase cloud database live - PostgreSQL + PostGIS 3.3, 22 churches seeded, spatial queries verified (2026-03-28)
- [x] PostGIS database migration - initial migration SQL with geography column, GIST spatial index, auto-sync trigger (2026-03-27)
- [x] Seed script rewrite - uses Prisma.Decimal, verifies PostGIS, tests spatial query, 5 sample reviews, admin user (2026-03-27)
- [x] ChurchCardSkeleton component - shimmer animation matching exact ChurchCard layout (2026-03-27)
- [x] Mapbox GL JS interactive map with Airbnb-style name-bubble pins, popups, and lazy loading (2026-03-27)
- [x] Map pin clustering - dark circles with count, click-to-zoom, three size tiers (2026-03-27)
- [x] Viewport-based querying - list + map sync via bounds parameter, debounced on pan/zoom (2026-03-27)
- [x] URL-based search state - shareable/bookmarkable URLs with all filters synced (2026-03-27)
- [x] Responsive layout - mobile-first list view, full-screen map/list toggle on small screens (2026-03-27)
- [x] No-results suggestions - contextual empty state with actionable filter-removal buttons (2026-03-27)
- [x] Keyboard navigation support for search results - focusable result cards, accessible search submit buttons, map popup CTA cleanup (2026-03-28)
- [x] Church Profile Page (F1.3) with hero, about, services, location, contact, amenities (2026-03-27)
- [x] Navigation wired: card clicks + map pin clicks -> profile page (2026-03-27)
- [x] Header updated with React Router links (2026-03-27)
- [x] CI pipeline fixed: added ESLint plugins, vitest/jest configs, smoke tests, removed premature Prisma step (2026-03-27)
- [x] GitHub repo created and initial code pushed - https://github.com/Mhartsuch/sa-church-finder (2026-03-27)
- [x] npm install and build verification - app runs end-to-end (2026-03-27)
- [x] Created server/.env with Docker DATABASE_URL (2026-03-27)
- [x] Fixed ChurchList onClick type mismatch and sort dropdown type safety (2026-03-27)
- [x] Project planning and architecture documentation (2026-03-26)
- [x] Data model design (2026-03-26)
- [x] API specification (2026-03-26)
- [x] Feature specifications (2026-03-26)
- [x] Coding conventions (2026-03-26)
- [x] Set up monorepo structure (`client/`, `server/`, shared scripts) (2026-03-26)
- [x] Initialize React + Vite + TypeScript frontend (2026-03-26)
- [x] Initialize Express + TypeScript backend (2026-03-26)
- [x] Create Prisma schema from `docs/engineering/DATA_MODELS.md` (2026-03-26)
- [x] Reorganize project documentation so supporting Markdown lives under `docs/` and the repo root stays focused on entry points (2026-03-31)
- [x] Write database seed script with 22 sample churches (2026-03-26)
- [x] Build backend search API with filtering, sorting, pagination (2026-03-26)
- [x] Build frontend search page with map + list layout (2026-03-26)
- [x] Create Zustand store for search state (2026-03-26)
- [x] Create React Query hooks for data fetching (2026-03-26)
- [x] Build ChurchCard component with hover interactions (2026-03-26)
- [x] Build placeholder map with SVG pins (2026-03-26)
- [x] Build search bar with debounce (2026-03-26)
- [x] Build filter panel (denomination, day, time, language, amenities) (2026-03-26)

---

## Backlog

- Church data import pipeline (Google Places API -> database)
- Email notification system (welcome, review responses, claim updates)
- Analytics dashboard for church admins
- A/B test different search result layouts
- Explore Elasticsearch if PostgreSQL FTS becomes a bottleneck
- Multi-city expansion architecture

## Known Bugs

- ~~Forum posts/replies invisible after creation~~ — **Fixed 2026-07-13**: the blanket `max-age=30, stale-while-revalidate=120` on all API GETs (plus the service worker's cache fallback) hid fresh writes; interactive reads are now `private, no-cache` and the SW bypasses interactive resources entirely
- ~~Category ribbon icons missing~~ — **Code fixed 2026-07-13**: client falls back to the built-in 6 categories; production still needs `db:seed:ribbon` + API redeploy (see P0)
- ~~LDS temple ranked at top of search~~ — **Code fixed 2026-07-13** (classifier + ranking demotion, DEC-024); production needs the reclassification run (see P0)

- ~~Flaky server test: `enrich-website-v2/html-cache.test.ts`~~ — **Fixed 2026-06-09**: `readHtmlCache` short-circuits non-positive max ages
- Production CSP likely blocks homepage Unsplash images and Supabase-hosted church photos — **config fixed 2026-06-09** in `render.yaml` (`img-src` now allows both hosts); verify on the live site once the static site redeploys
- ~~The Vercel preview-deploy workflow fails on every PR~~ — **Fixed 2026-06-09**: workflow deleted (hosting is Render)
- Live auth regression on the deployed app - a rerun from `https://sachurchfinder.com` shows the custom-domain CORS headers are now correct, but `POST /api/v1/auth/register` and `POST /api/v1/auth/login` still return `500 Internal Server Error`, so live auth, saved churches, and review mutations remain blocked until the backend is redeployed with the Prisma-backed session-store fix

- ~~Mapbox map not loading on Render (showing SVG placeholder instead)~~ — **Fixed 2026-03-29**: `VITE_MAPBOX_TOKEN` was missing from Render env vars; added to `render.yaml`. Token must be set in Render dashboard and a redeploy triggered.
- ~~4 moderate vulns in client, 1 moderate vuln in server after the dependency sweep~~ — **Mostly resolved 2026-06-09**: `npm audit fix` cleared every runtime vulnerability (server now audits clean; client runtime deps clean). The only remaining advisories are the dev-only `esbuild`/`vite`/`vite-node`/`vitest` chain (3 moderate + 1 critical), which affects the local dev server / Vitest UI only — never the production bundle — and requires a breaking `vite` 6 → 8 + `vitest` major upgrade to clear. Tracked as a deliberate deferral; revisit during the next planned tooling upgrade

---

_Last updated: 2026-07-13 (six-issue fix pass on `claude/church-finder-improvements-9budfk` — (1) slow load: render-blocking font @import replaced with preconnect+link, API-origin preconnect, hero preload, Sentry init deferred, SearchPage lazy-loaded (entry 154→87 kB), map-vendor off the eager preload set, SW cold-start timeout fallback for browse reads, scheduled keepalive workflow against the free-tier API; (2) search ranking: LDS-temple/JW classifier rules now precede the Catholic rules, 'San Antonio' as a city name no longer classifies as Catholic, and Latter-day Saints / Jehovah's Witnesses families are demoted (never excluded) in relevance+rating ordering per DEC-024, with new classifier and ranking tests; (3) category ribbon: CategoryFilter falls back to the built-in 6 chips on empty/error API responses and a new idempotent `db:seed:ribbon` script backfills production; (4) passport: create-collection modal, add-to-collection on church profiles, visit edit/delete, paginated full timeline, Header/MobileNav links — feature now complete end to end per spec; (5) forum: root cause was the blanket API GET cache header hiding fresh writes — interactive reads are now `private, no-cache` (with a new middleware test suite), the five missing forum indexes shipped as migration 20260713000000, createForumReply's type was corrected, and Forum joined the Header nav; (6) pipeline: new `pipeline:run` orchestrator (4 stages + stale-event cleanup), Places client retry/backoff + saturated-cell subdivision (the pageToken loop was dead code), needs_review status for low-confidence extractions, `--stale-days` freshness refresh, claimed churches never overwritten, `--reclassify-denominations` mode, v1 enrich-website deleted, all documented in docs/engineering/DATA_PIPELINE.md; full gate green; operational follow-ups captured as the new P0)_
_Last updated: 2026-06-09 (audit remediation pass — fixed everything actionable from the same-day audit: deterministic `html-cache` max-age guard; in-house 5-req/min/IP rate limiter on the seven credential endpoints with unit tests and an API_SPEC rewrite to match; `SESSION_SECRET` now required at startup in production; `compression` added to the API (the pass's single new dependency); production CSP `img-src` extended to Unsplash + Supabase hosts; robots.txt now declares the dynamic API sitemap and the stale static sitemap is gone; PWA icons (regular + maskable) generated from the SVG so the manifest and apple-touch-icon resolve; orphaned `site.webmanifest`, `client/vercel.json`, `server/src/data/churches.ts`, the broken `db:refresh-curated-data` script, and the failing Vercel preview workflow all deleted; `saved-church` test renamed to `.integration.test.ts`; footer fully routed with dead links and the fake language selector removed (tests updated); `client/.env.example` added and the server example corrected; `develop`-branch ghosts removed from `ci.yml`/`render.yaml`/BRANCHING_STRATEGY/ARCHITECTURE; vestigial `*.vercel.app` CORS allowance dropped; full gate green afterwards — client 490 tests, server 627 tests (4 new), lint/typecheck/build clean; branch cleanup remains blocked by the sandbox push proxy (command captured in the P3 item); the P0 Render redeploy + live smoke test remain operational follow-ups)_
_Last updated: 2026-06-09 (full-project audit, second pass — independently re-verified the gates (lint, typecheck, build green; tests 622/623 with one newly caught flaky test in `html-cache.test.ts`), confirmed zero open GitHub issues/PRs and green CI on `main`, then swept for issues the earlier audit missed: filed new P1s for the flaky test, the production CSP `img-src` allowlist that omits Unsplash + Supabase image hosts, the robots.txt/static-vs-dynamic sitemap conflict that hides church profiles from crawlers, the Vercel preview workflow that fails on every PR, and the absent API rate limiting that AGENTS.md/API_SPEC falsely advertise; filed P2s for the broken PWA icon set + orphaned `site.webmanifest`, the `SESSION_SECRET` dev fallback, API_SPEC/DATA_MODELS drift (5 undocumented endpoints, phantom `POST /churches`, undocumented `enrichment_states` table), the nonexistent `develop` branch referenced by CI/branching docs/Render staging config, dead code (`server/src/data/churches.ts`, `client/vercel.json`, broken `db:refresh-curated-data` script, misnamed `saved-church.routes.test.ts`), env-var documentation gaps (no `client/.env.example`), and eight dead footer links; filed P3s for the 48 stale `claude/*` remote branches, missing compression middleware, registration email enumeration, and the major-upgrade train; no product code changed — board update only)_
_Last updated: 2026-06-09 (finish-the-website audit + dependency vulnerability sweep — audited the full repo against all 8 feature specs in `specs/` and confirmed every one is implemented end to end (church passport/collections, denomination filter refinements, filter & search enhancements, leader features v2 review responses + service management, managed ribbon categories, search hardening + multi-select denomination, user account features, website enrichment pipeline v2 including its Jest coverage), confirmed there are no open GitHub issues/PRs and no TODO/FIXME/stub markers left in source, and re-verified the full gate green: client + server lint, typecheck, build, and tests (36 server suites / 623 tests plus the client Vitest suites) all pass; applied semver-compatible `npm audit fix` in both workspaces, which cleared all runtime vulnerabilities — server audits clean, and the client's only remaining advisories are the dev-only `esbuild`/`vite`/`vitest` chain that would need a breaking `vite` 6→8 major to clear, deferred deliberately; the two remaining open board items are operational, not code: trigger/verify the Render backend redeploy so live auth stops 500ing, then smoke test the live flows — both require Render dashboard or open-network access that the cloud agent sandbox does not have)_
_Last updated: 2026-04-16 (events discovery feed gains a verified-only / claimed-church filter — `GET /api/v1/events?verifiedOnly=true` (or `=1` / `=yes` via the shared `booleanishFlag` parser; anything else 400s) and `GET /api/v1/events.ics?verifiedOnly=true` restrict the JSON and iCalendar feeds to events at churches whose `isClaimed` flag is `true`, excluding unclaimed records so visitors know each listing has been confirmed by a church leader; when narrowed alongside the existing denomination / neighborhood / language / accessibleOnly / familyFriendly / groupFriendly church-level axes the seven sub-clauses `AND`-compose so subscribers see exactly the same set of events the discovery page surfaces; the calendar name and description gain a "verified churches" suffix and the `Content-Disposition` filename appends a static `verified` segment (e.g. `sa-church-finder-service-baptist-downtown-spanish-accessible-family-friendly-group-friendly-verified-events.ics`) so saved downloads stay self-describing; `buildAggregatedEventsFeedUrl` accepts a `verifiedOnly?: boolean | null` param that serializes only on `true`, the `/events` page exposes a BadgeCheck-icon toggle chip next to the existing Good-for-groups chip that syncs `verified=1` into the URL and renders a removable applied chip, and the EventsDiscoveryPage subscribe button gains an "axis-of-one" `Subscribe to verified-church events` label that collapses to the generic "Subscribe to the filtered events feed" wording when combined with another axis; the API spec's `/events` and `/events.ics` tables document the new param + filename suffix, 5 Jest cases cover the JSON feed's single-axis filter, the omitted / `=false` no-ops, the `=1` alias, and the four-boolean composition, 5 Jest cases cover the ICS feed's single-axis filter, the `=1` alias, the false no-op, the eight-axis `AND` composition verifying the filename chain, and the invalid-flag 400, and 8 Vitest cases cover the lib serialization (`true`, `false`, `null`, combined-with-other-axes), the discovery-page chip render/toggle/URL-seed/chip-clear flows, and the subscribe-button label + cross-axis URL composition)_
_Last updated: 2026-04-16 (aggregated calendar feed gains a keyword search filter — `GET /api/v1/events.ics?q=bible` narrows the iCalendar subscription to events whose title, description, or host church name contains the keyword (case-insensitive), mirroring the JSON feed contract so a visitor who has narrowed the discovery page with the search box can subscribe to exactly the same slice of the feed; Zod trims the input and caps it at 200 characters (anything longer 400s), and whitespace-only values collapse to "no filter" so `?q=` keeps the city-wide default; the service layer composes the keyword match as an `AND`-ed `OR` clause alongside the existing recurring/non-recurring window, so it coexists cleanly with the denomination / neighborhood / language / accessibleOnly / familyFriendly / groupFriendly church-level axes and the in-memory time-of-day narrowing without perturbing the church sub-clause; the calendar name and description append `matching "<keyword>"` and the `Content-Disposition` filename appends a slugified `matching-<keyword>` segment (e.g. `sa-church-finder-service-baptist-downtown-spanish-accessible-family-friendly-group-friendly-evening-matching-bible-events.ics`) so saved downloads stay self-describing; `buildAggregatedEventsFeedUrl` accepts a `q?: string | null` param that trims and serializes only when set, and the EventsDiscoveryPage subscribe button now mirrors `filters.q` into the calendar URL, gains an "axis-of-one" `Subscribe to events matching "<keyword>"` label, and collapses to the generic "Subscribe to the filtered events feed" wording when combined with another axis; the API spec's `/events.ics` table documents the new param + filename suffix, 5 Jest cases cover the single-axis filter with the expected `AND`-of-`OR` shape, whitespace trimming, the whitespace-only no-op, the nine-axis composition verifying the filename chain and the combined top-level `AND`, and the over-200-char 400, and 5 Vitest cases cover the lib serialization (single, whitespace-trimming, URL-encoded special characters, empty/null/undefined, combined-with-other-axes) plus 2 EventsDiscoveryPage cases verifying the labeled feed URL and the generic-label cross-axis URL)_
_Last updated: 2026-04-16 (aggregated calendar feed gains a time-of-day filter — `GET /api/v1/events.ics?timeOfDay=morning|afternoon|evening` restricts the iCalendar subscription to events whose stored `startTime` falls inside the named local-time bucket (San Antonio, `America/Chicago`), evaluated via the existing `matchesTimeOfDay` helper and applied in-memory on the rows Prisma returns so the narrowing composes cleanly with the existing denomination / neighborhood / language / accessibleOnly / familyFriendly / groupFriendly church-level axes without contributing its own `church` sub-clause; every RRULE occurrence of a recurring series shares the same hour-of-day, so filtering at the base row guarantees the emitted calendar still shows only in-bucket occurrences even though the calendar client expands the RRULE locally; the calendar name and description append the bucket label and the `Content-Disposition` filename appends a static `morning` / `afternoon` / `evening` segment (e.g. `sa-church-finder-service-baptist-downtown-spanish-accessible-family-friendly-group-friendly-evening-events.ics`) so saved downloads stay self-describing; `buildAggregatedEventsFeedUrl` accepts a `timeOfDay?: 'morning' | 'afternoon' | 'evening' | null` param that serializes only when set, and the EventsDiscoveryPage subscribe button now mirrors the time-of-day chip into the calendar URL, gains an "axis-of-one" "Subscribe to morning/afternoon/evening events" label, and collapses to the generic "Subscribe to the filtered events feed" wording when combined with another axis; the API spec's `/events.ics` table documents the new param + filename suffix, 5 Jest cases cover the morning / afternoon / evening single-bucket filters, the eight-axis composition verifying the filename chain and the `church.AND` clause, and the invalid-bucket 400, and 5 Vitest cases cover the lib serialization (`morning`, `afternoon`, `evening`, `null`, combined-with-other-axes) plus 2 EventsDiscoveryPage cases verifying the labeled feed URL and the generic-label cross-axis URL)_
_Last updated: 2026-04-16 (aggregated calendar feed gains a group-friendly filter — `GET /api/v1/events.ics?groupFriendly=true` (or `=1` / `=yes` via the shared `booleanishFlag` parser; anything else 400s) restricts the iCalendar subscription to events at churches whose `goodForGroups` flag is `true`, excluding both `false` and `null` (unknown) records via a nested `church.goodForGroups: true` clause in `getAggregatedCalendarFeed`; when narrowed alongside denomination / neighborhood / language / accessibleOnly / familyFriendly the six church-level sub-clauses `AND`-compose so subscribers see exactly the same set of events the discovery page surfaces; the calendar name and description gain a "good for groups" suffix and the `Content-Disposition` filename appends a static `group-friendly` segment (e.g. `sa-church-finder-service-baptist-downtown-spanish-accessible-family-friendly-group-friendly-events.ics`) so saved downloads stay self-describing; `buildAggregatedEventsFeedUrl` accepts a `groupFriendly?: boolean | null` param that serializes only on `true`, and the EventsDiscoveryPage subscribe button now mirrors the group-friendly chip into the calendar URL, gains an "axis-of-one" "Subscribe to group-friendly events" label, and collapses to the generic "Subscribe to the filtered events feed" wording when combined with another axis; the API spec's `/events.ics` table documents the new param + filename suffix, 5 Jest cases cover the single-axis filter, the boolean-ish `=1` alias, the `groupFriendly=false` no-op, the seven-axis `AND` composition, and the invalid-flag 400, and 3 Vitest cases cover the lib serialization (`true`, `false`, `null`, combined-with-other-axes) plus 2 EventsDiscoveryPage cases verifying the labeled feed URL and the generic-label cross-axis URL)_
_Last updated: 2026-04-16 (aggregated calendar feed gains a family-friendly filter — `GET /api/v1/events.ics?familyFriendly=true` (or `=1` / `=yes` via the shared `booleanishFlag` parser; anything else 400s) restricts the iCalendar subscription to events at churches whose `goodForChildren` flag is `true`, excluding both `false` and `null` (unknown) records via a nested `church.goodForChildren: true` clause in `getAggregatedCalendarFeed`; when narrowed alongside denomination / neighborhood / language / accessibleOnly the five church-level sub-clauses `AND`-compose so subscribers see exactly the same set of events the discovery page surfaces; the calendar name and description gain a "good for kids" suffix and the `Content-Disposition` filename appends a static `family-friendly` segment (e.g. `sa-church-finder-service-baptist-downtown-spanish-accessible-family-friendly-events.ics`) so saved downloads stay self-describing; `buildAggregatedEventsFeedUrl` accepts a `familyFriendly?: boolean | null` param that serializes only on `true`, and the EventsDiscoveryPage subscribe button now mirrors the kid-friendly chip into the calendar URL, gains an "axis-of-one" "Subscribe to kid-friendly events" label, and collapses to the generic "Subscribe to the filtered events feed" wording when combined with another axis; the API spec's `/events.ics` table documents the new param + filename suffix, 5 Jest cases cover the single-axis filter, the boolean-ish `=1` alias, the `familyFriendly=false` no-op, the six-axis `AND` composition, and the invalid-flag 400, and 3 Vitest cases cover the lib serialization (`true`, `false`, `null`, combined-with-other-axes) plus 2 EventsDiscoveryPage cases verifying the labeled feed URL and the generic-label cross-axis URL)_
_Last updated: 2026-04-16 (aggregated calendar feed gains a wheelchair-accessible filter — `GET /api/v1/events.ics?accessibleOnly=true` (`=1` / `=yes` also accepted via the shared `booleanishFlag` parser, anything else 400s) restricts the iCalendar subscription to events at churches whose `wheelchairAccessible` flag is `true`, excluding both `false` and `null` (unknown) records via a nested `church.wheelchairAccessible: true` clause in `getAggregatedCalendarFeed`; when narrowed alongside denomination/neighborhood/language the four church-level sub-clauses `AND`-compose so subscribers see exactly the same set of events the discovery page surfaces; the calendar name and description gain a "wheelchair accessible" suffix and the `Content-Disposition` filename appends a static `accessible` segment (e.g. `sa-church-finder-service-baptist-downtown-spanish-accessible-events.ics`) so saved downloads stay self-describing; `buildAggregatedEventsFeedUrl` accepts an `accessibleOnly?: boolean | null` param that serializes only on `true` and the EventsDiscoveryPage subscribe button now mirrors the wheelchair-accessible chip into the calendar URL, gains an "axis-of-one" "Subscribe to wheelchair-accessible events" label, and otherwise collapses to the generic "Subscribe to the filtered events feed" wording when combined with another axis; the API spec's `/events.ics` table documents the new param + filename suffix, 5 Jest cases cover the single-axis filter, the boolean-ish `=1` alias, the false no-op, the four-axis `AND` composition, and the invalid-flag 400, and 5 Vitest cases cover the lib serialization (`true`, `false`, `null`, omitted, and combined-with-other-axes) plus 2 EventsDiscoveryPage cases verifying the labeled feed URL and the generic-label cross-axis URL)_
_Last updated: 2026-04-16 (events discovery feed gains a service-language filter — `GET /api/v1/events?language=Spanish` (or `?language=English,Spanish` / repeated params) restricts the feed to events hosted by churches whose `languages` array contains any of the selected values via a Prisma `hasSome` clause on the shared nested `church` where-block; the `/events` page exposes a Languages-icon chip rail sourced from `GET /churches/filter-options` (`languages[]`) with up to 6 chips plus any URL-supplied language pinned to the front, round-trips the selection through the `language=…` URL param, renders a removable "Language: Spanish" chip per selection in the applied-chip rail, and preserves the existing chip-rail UX pattern used by denomination/event-type; the JSON feed echoes the deduped `language` list in `meta.filters`, the API spec documents the new param + response field, 5 Jest cases cover the single-value, multi-select, empty-meta, whitespace-stripping, and cross-filter-combination paths, and 6 Vitest cases verify chip rendering, multi-select URL serialization, chip-rail removal, URL round-tripping for unknown options, and the empty-languages fallback; the feature combines cleanly with the existing neighborhood, denomination, family-friendly, wheelchair-accessible, event-type, time-of-day, keyword, saved-only, and sort filters via the same nested `church` clause)_
_Last updated: 2026-04-16 (events discovery feed gains a "Good for kids" / family-friendly filter — `GET /api/v1/events?familyFriendly=true` restricts the feed to events at churches whose `goodForChildren` flag is `true` (the same nullable boolean already used for the church search hooks), excluding both `false` and `null` (unknown) churches so families can trust the narrowed list; the `/events` page exposes a Baby-icon toggle chip alongside the existing wheelchair-accessible chip with the same active/applied-chip pattern, syncs `family=1` into the URL, surfaces a removable applied chip in the chip rail, and combines with the existing `accessible=1`, neighborhood, denomination, and saved-only filters via the shared nested `church` clause; the JSON feed echoes `familyFriendly` in `meta.filters`, the API spec documents the new param + response field, and Jest/Vitest cover the filter alone, the false/empty no-op cases, and the cross-filter combination)_
_Last updated: 2026-04-16 (events discovery page gains a "This Sunday" quick-date preset chip — resolves to the upcoming Sunday's YYYY-MM-DD (or today when today is already a Sunday), slots into `EVENT_DATE_PRESETS` between Today and This weekend so visitors planning Sunday-service visits can pick it in one tap, round-trips through the same `from=`/`to=` URL params as the other presets, and stays mutually exclusive with the adjacent chips via the existing `matchEventDatePreset` highlighting; unit tests cover weekday, Saturday, and Sunday resolutions plus the preset match order, and a new EventsDiscoveryPage test mocks the system clock to verify the chip syncs `2026-04-19` into the URL and flips to `aria-pressed=true`)_
_Last updated: 2026-04-15 (events discovery feed now supports filtering by neighborhood — `GET /api/v1/events?neighborhood=Downtown` restricts the feed to events from churches in the named San Antonio neighborhood using a case-insensitive match against the church's stored `neighborhood` column; the `/events` page exposes a neighborhood select populated from the existing `/churches/filter-options` endpoint, URL-syncs the selection as `neighborhood=`, renders a removable active-filter chip, and preserves out-of-cache URL values so shared links keep working; the server combines neighborhood with `savedOnly` into a single nested `church` where-clause and caps the value at 120 chars; multi-select event types, day grouping, time-of-day, saved-church, keyword, and date-preset filters all continue to work alongside the new select; `IEventChurchSummary` now carries `neighborhood` so aggregated event payloads expose it; `sachurchfinder.com` still needs a backend redeploy for the Prisma session-store auth fix)_
